/**
 * You May Also Like — decoration pending Universal Editor HTML (see implementation plan).
 * Phase 3: implement decorate(block) from user-provided markup.
 */
export default function decorate(/* block */) {
  // intentionally empty until structure contract from UE is available
}
