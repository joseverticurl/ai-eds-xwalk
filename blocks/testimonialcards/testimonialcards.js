/**
 * Testimonial Cards Block
 *
 * Placeholder: Full implementation requires user-provided HTML from Universal Editor (Phase 0.5).
 * Structure contract (from XWalk model; to be validated against user HTML):
 * - block.children[0] = Section title line 1 row
 * - block.children[1] = Section title line 2 row
 * - block.children[2] = Description row
 * - block.children[3] = CTA label row
 * - block.children[4] = CTA link row
 * - block.children[5] = Background option row
 * - block.children[6+] = Card item rows
 *
 * Per item row: image, imageAlt, name, role, quote, mediaType, mediaSource,
 * mediaThumbnail, errorMessageOverride
 *
 * @param {Element} block The block element
 */
export default function decorate(block) {
  // Placeholder: no-op until user-provided HTML is received and full implementation.
  // Block will display raw AEM-generated HTML until decorate() is fully implemented.
  block.classList.add('testimonialcards-placeholder');
}
