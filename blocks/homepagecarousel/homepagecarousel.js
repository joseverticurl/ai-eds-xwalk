/**
 * Homepage Carousel
 *
 * Phase 1: XWalk models only. Implement `decorate()` after Universal Editor HTML
 * defines the structure contract (see implementation plan Phase 2).
 *
 * @param {Element} block The block element
 */
export default async function decorate(block) {
  await Promise.resolve(block);
}
