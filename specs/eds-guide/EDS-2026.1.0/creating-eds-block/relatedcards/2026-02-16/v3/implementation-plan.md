# Implementation Plan: Related Cards Block

**Functionality:** Related Cards Block (You May Also Like)  
**Date:** 2026-02-16  
**Version:** v3  
**Guide Reference:** [Creating EDS Block Implementation Guide](../../implementation-guide.md)

---

## Overview

This implementation plan details the creation of a **Related Cards** block that displays a configurable section title and multiple content cards. Each card includes an image with gradient overlay, a category badge, a title, and a link. The block supports responsive layouts with different interaction patterns: desktop (title clickable) and mobile (title + "Read more" button clickable). Cards maintain aspect ratio and are not fully clickable.

### User Story

**As a** website visitor  
**I want to** see related content recommendations  
**So that** I can easily discover additional relevant articles or pages.

### Acceptance Criteria

**General:**
- Component displays a configurable section title (e.g., "You may also like")
- Component supports multiple content cards
- Each card includes:
  - Image (required)
  - Category/tag label (authorable, required)
  - Title (required)
  - CTA link (required)
- Content is authorable in AEM
- Component is reusable and configurable
- Responsive design (desktop horizontal, mobile vertical)
- Cards maintain aspect ratio (not fixed height)

**Desktop Experience:**
- Cards display in a horizontal layout
- Multiple cards visible at once
- Consistent spacing and alignment (20px gap between cards)
- **Title is clickable** (not entire card)
- Component fits within the page grid

**Mobile Experience:**
- Cards display in a vertical stacked layout
- One card per row
- Content remains readable without truncation
- **Title is clickable + "Read more" button is clickable** (not entire card)
- No horizontal scrolling

**Accessibility:**
- Images include alt text
- Links are keyboard accessible
- Color contrast meets accessibility standards

**Performance:**
- Images are responsive and optimized
- Component loads efficiently without impacting page performance

---

## Design Specifications

### Figma Design Analysis

**Source:** [Figma Design](https://www.figma.com/design/1uWBLEcq2rARuQXFnsqdQD/Components---TCCC?node-id=1-1691&m=dev)

**Layout Structure:**
- Section title: "You may also like" (centered, large heading)
- Three cards displayed horizontally on desktop
- Each card: 440px width (height maintains aspect ratio)
- Gap between cards: 20px

**Card Structure:**
- Background image with gradient overlay (transparent to black, 0.6 opacity)
- Badge/tag in top-left corner (white background, rounded pill shape, 50px border-radius)
- Title text at bottom (white, multi-line)
- Rounded corners: 24px border radius
- Padding: 30px

**Typography:**
- Section title: TCCC-UnityHeadline Medium, 64px, line-height 1.2, letter-spacing -3px
- Card title: TCCC-UnityHeadline Medium, 26px, line-height 1.2
- Badge: TCCC-UnityHeadline Medium, 12px, line-height 1.4

**Colors:**
- Background: Off-white (#f2f2f2)
- Text: Black (#000000) for section title, White for card titles
- Badge: White background (#ffffff) with black text (#000000)
- Gradient: `linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgba(0,0,0,0.6) 100%)`

**Responsive Breakpoints:**
- Desktop: 900px and above (horizontal layout, title clickable)
- Mobile: Below 900px (vertical stacked layout, title + "Read more" button clickable)

**Interactions:**
- **Desktop:** Title text is clickable (not entire card)
- **Mobile:** Title text is clickable + "Read more" button is clickable (not entire card)
- Hover effect on clickable elements: opacity change or underline

---

## Implementation Phases

### Phase 0.5: Pre-Implementation - Create Static HTML for Testing (MANDATORY)

**Objective:** Create static HTML files based on actual AEM-generated HTML structure for local testing and structure validation.

**Steps:**
1. **Configure XWalk Model in AEM:**
   - [ ] Add component definition to `component-definition.json`
   - [ ] Add model to `component-models.json`
   - [ ] Add filter to `component-filters.json` (if needed)
   - [ ] Deploy to AEM or test in AEM environment

2. **Author Content in AEM:**
   - [ ] Add the block to a page in AEM
   - [ ] Configure all fields (including empty/optional fields)
   - [ ] Add multiple items if it's a parent-child block
   - [ ] Test with various field combinations:
     - [ ] With all fields filled
     - [ ] With optional fields (e.g., section title) empty
     - [ ] With some fields empty
     - [ ] With multiple child items (3 cards)

3. **Extract Actual HTML Structure:**
   - [ ] View page source or use browser DevTools
   - [ ] Copy the actual HTML structure generated by AEM
   - [ ] Include all variations (with/without optional fields, multiple items, etc.)
   - [ ] Document observations:
     - [ ] Which fields generate rows vs cells
     - [ ] Field indices (0, 1, 2, etc.)
     - [ ] Empty field behavior (missing cells vs empty cells)
     - [ ] Optional field behavior (parent title, etc.)
     - [ ] How alt text is handled (separate cell vs img attribute)
     - [ ] **CRITICAL:** Verify if `imageAlt` field generates a separate cell or is only in `<img>` tag's `alt` attribute
     - [ ] Document actual cell count per card row (expected: 5 cells, not 6 if imageAlt doesn't generate a cell)

4. **Create Static HTML Files:**
   - [x] Create folder: `specs/eds-guide/EDS-2026.1.0/creating-eds-block/relatedcards/static-html/`
   - [x] Create `test-structure.html` with basic structure (all fields, 3 cards)
   - [x] Create additional test files as needed:
     - [x] `test-with-title.html` (if parent has optional title)
     - [x] `test-without-title.html` (if parent title is optional)
     - [ ] `test-empty-fields.html` (with some empty fields)
   - [x] Add comments documenting structure contract in each file

5. **Validate Structure Contract:**
   - [ ] Compare actual HTML with expected structure from XWalk model
   - [ ] Document actual field indices
   - [ ] Document empty field behavior
   - [ ] Document optional field behavior
   - [ ] Update structure contract documentation if needed

6. **Test with Static HTML:**
   - [ ] Load static HTML in browser
   - [ ] Test JavaScript extraction logic (manually verify indices)
   - [ ] Verify index-based access works correctly
   - [ ] Test CSS styling
   - [ ] Fix any structure mismatches before final implementation

**Important:**
- Static HTML files are temporary development artifacts
- Location: `specs/.../static-html/` folder (NOT in EDS project root)
- Purpose: Local testing and structure validation
- **MANDATORY:** Request user to delete all static HTML files after implementation is complete

**Testing Task:**
- [ ] **HUMAN TEST:** Verify static HTML structure matches actual AEM output
- [ ] **HUMAN TEST:** Document actual field indices and structure contract
- [ ] **HUMAN TEST:** Validate that structure contract is clear and unambiguous

**Confidence:** 100% - This phase validates assumptions before coding.

---

### Phase 1: XWalk Configuration - Component Definition

**Objective:** Register `relatedcards` (parent) and `relatedcard` (child) components in AEM's component browser.

**Steps:**
1. Open `component-definition.json`
2. Locate the `"Blocks"` group
3. Add `relatedcards` parent component definition
4. Add `relatedcard` child component definition

**Component Definitions:**
- **Parent:** `relatedcards` - Uses `block` resourceType with `RelatedCards` template
  - **CRITICAL:** Since parent has authoring fields (title), it MUST have BOTH:
    - `model: "relatedcards"` - Enables authoring fields for parent block
    - `filter: "relatedcards"` - Allows child `relatedcard` items to be nested
- **Child:** `relatedcard` - Uses `block/item` resourceType with `RelatedCard` template and `relatedcard` model

**Template Configuration:**
```json
// Parent block (with authoring fields):
"template": {
  "name": "RelatedCards",
  "model": "relatedcards",    // ✅ REQUIRED - enables parent authoring fields
  "filter": "relatedcards"     // ✅ REQUIRED - allows child items
}

// Child block:
"template": {
  "name": "RelatedCard",
  "model": "relatedcard"      // ✅ REQUIRED - defines child item fields
}
```

**Reference:** Implementation guide Part 1: "XWalk Configuration - Component Definition" - "Block with Items (Parent + Child)"

**Validation:**
- [ ] Valid JSON syntax
- [ ] Both components added to `"Blocks"` group
- [ ] Parent uses `block` resourceType
- [ ] Child uses `block/item` resourceType
- [ ] **CRITICAL:** Parent template has BOTH `model` and `filter` (since parent has authoring fields)
- [ ] Child template has `model`
- [ ] `model` values match the `id` in `component-models.json`

**Testing Task:**
- [ ] **HUMAN TEST:** Run `npm run build:json` (if project uses build pipeline) to verify JSON files compile without errors
- [ ] **HUMAN TEST:** Open AEM authoring interface and verify `Related Cards` appears in component browser
- [ ] **HUMAN TEST:** Verify you can add `Related Card` items as children of `Related Cards` block

**Confidence:** 95% - Configuration follows established patterns, but requires AEM validation.

---

### Phase 2: XWalk Configuration - Component Models

**Objective:** Define data models for `relatedcards` (parent) and `relatedcard` (child) components.

**Steps:**
1. Open `component-models.json`
2. Add `relatedcards` model (parent - section title)
3. Add `relatedcard` model (child - image, alt text, badge, title, link, link target)

**Model Structure:**

**Parent Model (`relatedcards`):**
- `title` (text, optional) - Section title

**Child Model (`relatedcard`):**
- `image` (reference, required) - Image asset
- `imageAlt` (text, optional) - Alt text for image
- `badge` (text, required, maxLength: 50) - Badge text
- `title` (text, required, maxLength: 200) - Card title
- `link` (aem-content, required) - Link URL
- `linkTarget` (text, default: "_self", validation: "_self" or "_blank") - Link target

**CRITICAL - Field Order:**
The order of fields in `component-models.json` determines the index-based structure contract:
- Parent: title (index 0)
- Child: image (0), badge (1), title (2), link (3), linkTarget (4)

**IMPORTANT - Alt Text Field:**
- The `imageAlt` field in the model does NOT generate a separate cell in AEM HTML output
- Alt text is stored in the `<img>` tag's `alt` attribute, not as a separate cell
- JavaScript must extract alt text from: `imgElement?.getAttribute?.('alt') || ''`
- Do NOT expect `cells[1]` to be alt text - it will be the badge field
- Actual card row structure: 5 cells (image, badge, title, link, target) - NOT 6 cells

**Reference:** Implementation guide Part 1: "XWalk Configuration - Component Models" and "Field Configuration - Order Matters"

**Validation:**
- [ ] Valid JSON syntax
- [ ] Both models added
- [ ] Field order matches structure contract
- [ ] Validation rules applied (maxLength, regExp)

**Testing Task:**
- [ ] **HUMAN TEST:** Verify field order in AEM authoring UI matches model definition
- [ ] **HUMAN TEST:** Test validation rules (maxLength, linkTarget values)

**Confidence:** 95% - Following established patterns, but requires AEM validation.

---

### Phase 3: XWalk Configuration - Component Filters

**Objective:** Configure nesting rules to allow `relatedcard` items within `relatedcards` and `relatedcards` within `section`.

**Steps:**
1. Open `component-filters.json`
2. Add `relatedcards` filter allowing `relatedcard` as child
3. Update `section` filter to allow `relatedcards` as child

**Filter Configuration:**
- `relatedcards` filter: allows `relatedcard` components
- `section` filter: add `relatedcards` to allowed components

**Reference:** Implementation guide Part 1: "XWalk Configuration - Component Filters"

**Validation:**
- [ ] Valid JSON syntax
- [ ] `relatedcards` filter added
- [ ] `section` filter updated

**Testing Task:**
- [ ] **HUMAN TEST:** Verify you can add `Related Card` items inside `Related Cards` block
- [ ] **HUMAN TEST:** Verify you can add `Related Cards` block inside a section

**Confidence:** 95% - Configuration follows established patterns, but requires AEM validation.

---

### Phase 4: Validate XWalk Configuration

**Objective:** Verify all XWalk configuration files are correct and components are registered.

**Steps:**
1. Verify JSON syntax for all three files
2. Verify component registration
3. Verify field order matches structure contract

**Validation Checklist:**
- [ ] `component-definition.json` - Valid JSON
- [ ] `component-models.json` - Valid JSON
- [ ] `component-filters.json` - Valid JSON
- [ ] `relatedcards` definition present
- [ ] `relatedcard` definition present
- [ ] `relatedcards` model present
- [ ] `relatedcard` model present
- [ ] `relatedcards` filter present
- [ ] Field order matches structure contract:
  - [ ] Parent model: title at index 0
  - [ ] Child model: image(0), badge(1), title(2), link(3), linkTarget(4)
  - [ ] **CRITICAL:** Verified that `imageAlt` field does NOT generate a separate cell (alt text is in `<img>` tag's `alt` attribute)

**Testing Task:**
- [ ] **HUMAN TEST:** Run `npm run build:json` (if project uses build pipeline) to verify JSON files compile without errors
- [ ] **HUMAN TEST:** Open AEM authoring interface and verify `Related Cards` appears in component browser
- [ ] **HUMAN TEST:** Add `Related Cards` block to a page and verify it appears in authoring UI
- [ ] **HUMAN TEST:** Verify you can add `Related Card` items as children of `Related Cards` block

**Confidence:** 95% - Configuration follows established patterns, but requires AEM validation.

---

### Phase 5: Frontend - JavaScript Implementation (ONE Folder Pattern)

**Objective:** Create `blocks/relatedcards/relatedcards.js` to handle section title AND all child card items in ONE `decorate()` function.

**CRITICAL:** Following the implementation guide, parent-child blocks use ONE folder with ONE JavaScript file. The `decorate()` function processes both the parent section title (if present) and all child card items.

**Steps:**
1. Create directory: `blocks/relatedcards/`
2. Create file: `blocks/relatedcards/relatedcards.js`
3. Implement the `decorate()` function following index-based pattern

**Structure Contract (based on actual AEM output):**
- `block.children[0]` = Section title row (if title field is provided, optional)
  - **Detection:** If first row has image cell (picture/img), it's a card row, not title row
- `block.children[1+]` = Related card item rows (each row = one item)

**For each card item row (actual AEM structure - 5 cells, NOT 6):**
- `row.children[0]` = Image cell (contains picture/img)
- `row.children[1]` = Badge text cell (NOT alt text - alt text is in img tag)
- `row.children[2]` = Title cell
- `row.children[3]` = Link URL cell (contains anchor tag)
- `row.children[4]` = Link target cell (text: "_self" or "_blank")

**CRITICAL - Alt Text Extraction:**
- Alt text is NOT a separate cell - it's in the `<img>` tag's `alt` attribute
- Extract using: `const imgElement = imageCell?.querySelector?.('img'); const imageAlt = imgElement?.getAttribute?.('alt') || '';`
- Do NOT try to extract from `cells[1]` - that's the badge field

**Key Requirements:**
- ONE folder: `blocks/relatedcards/`
- ONE JS file: `relatedcards.js` with ONE `decorate()` function
- Processes parent title (if present) AND all child card items in the same function
- Card is a `<div>`, not an `<a>` (card is not fully clickable)
- Desktop: Title link is clickable
- Mobile: Title link + "Read more" button are clickable
- Use `moveInstrumentation()` when transforming DOM
- Extract data using index-based access only (validated with static HTML)
- Use `createOptimizedPicture()` for image optimization
- **CRITICAL:** Extract alt text from `<img>` tag's `alt` attribute, NOT from a separate cell
- **CRITICAL:** Field indices: badge=1, title=2, link=3, target=4 (NOT badge=2, title=3, etc.)

**Reference:** 
- `blocks/relatedarticles/relatedarticles.js` - Similar parent-child pattern (ONE folder, ONE JS)
- Implementation guide Part 1: "Critical: Parent-Child Blocks Use ONE Folder"
- Static HTML files for structure contract validation

**Validation:**
- [x] File created in correct location (`blocks/relatedcards/relatedcards.js`)
- [x] Default export function `decorate(block)`
- [x] Structure contract documented in JSDoc (based on actual static HTML)
- [x] Index-based access used (no data attributes)
- [x] Processes parent title AND child items in same function
- [x] Card is `<div>`, not `<a>` (not fully clickable)
- [x] Desktop: Title link is clickable
- [x] Mobile: Title link + "Read more" button are clickable
- [x] `moveInstrumentation()` used when replacing elements
- [x] Image optimization implemented

**Testing Task:**
- [ ] **HUMAN TEST:** Open page in browser and verify cards render correctly
- [ ] **HUMAN TEST:** Verify desktop: only title is clickable (not entire card)
- [ ] **HUMAN TEST:** Verify mobile: title and "Read more" button are both clickable
- [ ] **HUMAN TEST:** Verify images load and display correctly
- [ ] **HUMAN TEST:** Verify gradient overlay appears on cards
- [ ] **HUMAN TEST:** Verify all 3 cards render (not just 2)

**Confidence:** 90% - Following established patterns, but structure contract must be validated with static HTML first.

---

### Phase 6: Frontend - CSS Implementation (ONE Folder Pattern)

**Objective:** Create `blocks/relatedcards/relatedcards.css` with styles for both parent container and child card items.

**CRITICAL:** Following the implementation guide, parent-child blocks use ONE folder with ONE CSS file.

**Steps:**
1. Create file: `blocks/relatedcards/relatedcards.css`
2. Implement styles for parent container (section title)
3. Implement styles for child card items
4. Implement responsive breakpoints (desktop: 900px+, mobile: <900px)

**Key Requirements:**
- ONE folder: `blocks/relatedcards/`
- ONE CSS file: `relatedcards.css`
- Styles for parent container (`.related-cards__container`, `.related-cards__title`, `.related-cards__items`)
- Styles for child cards (`.related-card`, `.related-card__image-wrapper`, `.related-card__gradient`, `.related-card__content`, `.related-card__badge`, `.related-card__title-link`, `.related-card__read-more`)
- Responsive design: desktop horizontal layout, mobile vertical layout
- "Read more" button hidden on desktop, visible on mobile
- Cards maintain aspect ratio (not fixed height)
- Gradient overlay on images
- Typography matches Figma design

**Reference:**
- Implementation guide Part 1: "Critical: Parent-Child Blocks Use ONE Folder"
- Figma design specifications
- Static HTML files for structure validation

**Validation:**
- [x] File created in correct location (`blocks/relatedcards/relatedcards.css`)
- [x] Styles for parent container
- [x] Styles for child cards
- [x] Responsive breakpoints implemented
- [x] "Read more" button visibility (hidden desktop, visible mobile)
- [x] Typography matches design
- [x] Colors match design
- [x] Spacing matches design

**Testing Task:**
- [ ] **HUMAN TEST:** Verify desktop layout (horizontal, 3 cards visible)
- [ ] **HUMAN TEST:** Verify mobile layout (vertical, one card per row)
- [ ] **HUMAN TEST:** Verify "Read more" button is hidden on desktop
- [ ] **HUMAN TEST:** Verify "Read more" button is visible on mobile
- [ ] **HUMAN TEST:** Verify gradient overlay appears on cards
- [ ] **HUMAN TEST:** Verify typography matches Figma design
- [ ] **HUMAN TEST:** Verify colors match Figma design
- [ ] **HUMAN TEST:** Verify spacing matches Figma design

**Confidence:** 95% - Following established patterns and design specifications.

---

### Phase 7: Integration - Block Auto-Loading (No Manual Registration)

**Objective:** Verify the `relatedcards` block loads automatically in EDS.

**CRITICAL:** Blocks in EDS are auto-loaded based on class name. No manual registration is required.

**How EDS Auto-Loads Blocks:**
- EDS automatically detects blocks by class name (e.g., `relatedcards`)
- When a block with class `relatedcards` is found, EDS automatically:
  - Loads `blocks/relatedcards/relatedcards.css`
  - Loads `blocks/relatedcards/relatedcards.js`
  - Calls `decorate(block)` function
- This happens automatically via `loadBlock()` function in `scripts/aem.js`

**Steps:**
1. Verify block files exist:
   - `blocks/relatedcards/relatedcards.js` exists
   - `blocks/relatedcards/relatedcards.css` exists
2. Verify block class name matches folder name:
   - Folder: `blocks/relatedcards/`
   - Class: `relatedcards` (must match)
   - File: `relatedcards.js` (must match)

**Reference:** `scripts/aem.js` - `loadBlock()` function (lines 574-605)

**Validation:**
- [ ] Block files exist in correct location
- [ ] File names match block class name
- [ ] `decorate(block)` is default export
- [ ] No manual registration needed (auto-loaded)

**Testing Task:**
- [ ] **HUMAN TEST:** Add block to AEM page with class `relatedcards`
- [ ] **HUMAN TEST:** Verify block loads on pages automatically
- [ ] **HUMAN TEST:** Verify block JavaScript executes
- [ ] **HUMAN TEST:** Verify block CSS applies

**Confidence:** 100% - Blocks are auto-loaded by EDS, no manual registration needed.

---

### Phase 8: AEM Authoring Validation

**Objective:** Verify the block works correctly in AEM authoring interface.

**Steps:**
1. Test component in AEM authoring mode
2. Verify all fields are authorable
3. Verify validation rules work
4. Verify component preview renders correctly

**Validation Checklist:**
- [ ] Component appears in component browser
- [ ] Can add component to page
- [ ] All fields are authorable
- [ ] Validation rules work (maxLength, linkTarget)
- [ ] Component preview renders correctly
- [ ] Can add multiple child items (3+ cards)
- [ ] Can configure section title (optional)

**Testing Task:**
- [ ] **HUMAN TEST:** Add component to AEM page
- [ ] **HUMAN TEST:** Configure all fields
- [ ] **HUMAN TEST:** Add 3 child card items
- [ ] **HUMAN TEST:** Verify component preview renders correctly
- [ ] **HUMAN TEST:** Test with empty optional fields
- [ ] **HUMAN TEST:** Test validation rules

**Confidence:** 90% - Requires AEM environment testing.

---

### Phase 9: Responsive Testing

**Objective:** Verify the block renders correctly at all breakpoints.

**Steps:**
1. Test desktop layout (900px+)
2. Test mobile layout (<900px)
3. Test tablet layout (768px-900px)
4. Verify interaction patterns (clickable elements)

**Validation Checklist:**
- [ ] Desktop: Horizontal layout, 3 cards visible
- [ ] Desktop: Title is clickable (not entire card)
- [ ] Mobile: Vertical layout, one card per row
- [ ] Mobile: Title and "Read more" button are clickable
- [ ] Tablet: Appropriate layout
- [ ] All breakpoints: Cards maintain aspect ratio
- [ ] All breakpoints: Gradient overlay visible

**Testing Task:**
- [ ] **HUMAN TEST:** Test desktop viewport (1920px, 1440px, 1280px)
- [ ] **HUMAN TEST:** Test tablet viewport (768px, 900px)
- [ ] **HUMAN TEST:** Test mobile viewport (375px, 414px, 375px)
- [ ] **HUMAN TEST:** Verify clickable elements work at all breakpoints
- [ ] **HUMAN TEST:** Verify layout doesn't break at any breakpoint

**Confidence:** 90% - Requires browser testing.

---

### Phase 10: Accessibility Testing

**Objective:** Verify the block meets accessibility standards.

**Steps:**
1. Test keyboard navigation
2. Test screen reader compatibility
3. Verify color contrast
4. Verify alt text on images
5. Verify ARIA labels

**Validation Checklist:**
- [ ] Keyboard navigation works (Tab, Enter, Space)
- [ ] Screen reader announces content correctly
- [ ] Color contrast meets WCAG AA standards
- [ ] Images have alt text
- [ ] Links have descriptive text
- [ ] Focus indicators visible

**Testing Task:**
- [ ] **HUMAN TEST:** Test keyboard navigation (Tab through all links)
- [ ] **HUMAN TEST:** Test with screen reader (NVDA, JAWS, VoiceOver)
- [ ] **HUMAN TEST:** Verify color contrast (use browser extension)
- [ ] **HUMAN TEST:** Verify alt text on images
- [ ] **HUMAN TEST:** Verify focus indicators

**Confidence:** 90% - Requires accessibility testing tools.

---

### Phase 11: Performance Testing

**Objective:** Verify the block loads efficiently without impacting page performance.

**Steps:**
1. Test image optimization
2. Test lazy loading
3. Test page load performance
4. Verify no layout shift

**Validation Checklist:**
- [ ] Images are optimized (responsive, WebP)
- [ ] Images lazy load
- [ ] No layout shift (CLS)
- [ ] Block loads quickly
- [ ] No performance regressions

**Testing Task:**
- [ ] **HUMAN TEST:** Test image optimization (check Network tab)
- [ ] **HUMAN TEST:** Test lazy loading (scroll to block)
- [ ] **HUMAN TEST:** Test page load performance (Lighthouse)
- [ ] **HUMAN TEST:** Verify no layout shift

**Confidence:** 90% - Requires performance testing tools.

---

### Phase 12: Final Validation

**Objective:** Verify the block matches design specifications and acceptance criteria.

**Steps:**
1. Compare with Figma design
2. Verify all acceptance criteria met
3. Test edge cases
4. Verify cleanup tasks

**Validation Checklist:**
- [ ] Matches Figma design (layout, typography, colors, spacing)
- [ ] All acceptance criteria met
- [ ] Edge cases handled (empty fields, missing images, etc.)
- [ ] Static HTML files deleted (cleanup)

**Testing Task:**
- [ ] **HUMAN TEST:** Compare with Figma design (pixel-perfect)
- [ ] **HUMAN TEST:** Verify all acceptance criteria met
- [ ] **HUMAN TEST:** Test edge cases (empty fields, missing images, etc.)
- [ ] **HUMAN TEST:** Delete all temporary static HTML files from `specs/.../static-html/` folder

**Confidence:** 95% - Final validation before completion.

---

## Summary

This implementation plan follows a test-driven development approach with mandatory static HTML creation for structure validation. The plan includes:

1. **Phase 0.5 (MANDATORY):** Create static HTML from actual AEM output for structure validation
2. **Phases 1-4:** XWalk configuration (definition, models, filters, validation)
3. **Phases 5-6:** Frontend implementation (JavaScript and CSS, ONE folder pattern)
4. **Phase 7:** Integration (verify block auto-loads - no manual registration needed)
5. **Phases 8-12:** Testing and validation (AEM authoring, responsive, accessibility, performance, final validation)
6. **Cleanup:** Delete temporary static HTML files

**Key Principles:**
- **ONE Folder Pattern:** Parent-child blocks use ONE folder with ONE JS and ONE CSS file
- **Index-Based Access:** No data attributes, use DOM structure and indices
- **Structure Contract:** Validate actual AEM output with static HTML before coding
- **Test-Driven:** Human testing after logical steps
- **Cleanup:** Delete temporary static HTML files after implementation

**Next Steps:**
1. Execute Phase 0.5: Create static HTML from actual AEM output
2. Validate structure contract with static HTML
3. Proceed with XWalk configuration (Phases 1-4)
4. Implement frontend code (Phases 5-6) based on validated structure contract
5. Complete integration and testing (Phases 7-12)
6. Cleanup: Delete temporary static HTML files

---

## Notes

- **Static HTML is MANDATORY:** Do not skip Phase 0.5. Create static HTML from actual AEM output before writing JavaScript and CSS.
- **Structure Contract:** The actual HTML structure may differ from the XWalk model. Always validate with static HTML.
- **ONE Folder Pattern:** Parent-child blocks must use ONE folder with ONE JS and ONE CSS file.
- **Index-Based Access:** Never use data attributes. Always use DOM structure and indices.
- **Cleanup:** Remember to delete temporary static HTML files after implementation is complete.
